# Changelog 标准模板

> 每次更新 changelog.html 前必读此文件，按此模板填写。

## 一、格式规范

### 1. 版本块结构（每个版本照抄）

```html
<div class="cl-ver">vX.X.X · YYYY/M/DD</div>
<p class="cl-sub">一句话副标题</p>
<div class="cl-cat">一、分类名</div>
<ul class="cl-list">
<li><span class="cl-tag cl-xxx">标签</span>内容描述</li>
</ul>
<hr class="cl-hr">
```

### 2. 标签对照表

| 标签 | CSS 类   | 颜色 | 用途          |
| -- | ------- | -- | ----------- |
| 新增 | cl-new  | 绿色 | 新功能、新模块、新选项 |
| 修复 | cl-fix  | 红色 | Bug 修复      |
| 优化 | cl-opt  | 蓝色 | 改进、调整、性能提升  |
| 删除 | cl-del  | 灰色 | 删除、移除、清理    |
| 重构 | cl-ref  | 紫色 | 代码重构、架构调整   |
| 计划 | cl-plan | 橙色 | 未来计划、待办     |

### 3. 分类命名习惯

- `一、新增功能` — 新增的东西
- `二、优化修复` — 优化和修复混在一起
- `三、系统重构` — 重构相关
- `四、未来计划` — 计划中的事

分类不固定，按实际内容起名，比如：

- `一、优化修复`
- `一、新增功能`
- `一、重大重构`

### 4. 副标题

一句话概括这个版本干了啥，口语化就行，比如：

- `修复已知问题`
- `公众号生成功能正式上线`
- `深色模式全面修复`
- `开源库集成`
- `你说什么啊？`
- `吃了一块提拉米苏🍰`

### 5. 条目写法

- 标签后直接写内容，不要加冒号
- 内容简洁，一句话说清楚
- 不要写技术细节（代码路径、函数名等），那是 commit message 的事
- 例子：
  - ✅ `修复 索引重建后容器崩溃问题`
  - ❌ `修复 api_v2.py 中 _do_rebuild 函数 ChromaDB OOM 导致容器崩溃问题`

## 二、空白模板（复制即用）

```html
<div class="cl-ver">vX.X.X · YYYY/M/DD</div>
<p class="cl-sub">副标题</p>
<div class="cl-cat">一、分类名</div>
<ul class="cl-list">
<li><span class="cl-tag cl-new">新增</span>功能描述</li>
<li><span class="cl-tag cl-fix">修复</span>修复描述</li>
<li><span class="cl-tag cl-opt">优化</span>优化描述</li>
</ul>
<hr class="cl-hr">
```

## 三、注意事项

1. 新版本插在 `<style>` 块之后、旧版本之前
2. 日期格式 `YYYY/M/DD`（不要补零，如 `2026/6/25` 不是 `2026/06/25`）
3. 版本号格式 `vX.X.X`（如 `v4.7.4`）
4. 没有前缀词的条目不加标签，直接写 `<li>内容</li>`
5. 条目内容不要太高深，用户能看懂就行
6. **【重要】标签必须与分类匹配**：
   - `新增` 标签 → 必须放在 `新增功能` 分类下
   - `修复` 标签 → 必须放在 `优化修复 `分类下
   - `优化` 标签 → 必须放在 `优化修复` 分类下
   - 不要把 `新增` 标签混在 `优化修复` 分类里！

